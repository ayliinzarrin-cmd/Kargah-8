# Team Work and GitHub Guide

Repository link:

```text
https://github.com/ayliinzarrin-cmd/Kargah-8
```

## Project Branches and Task Plan

### Member 1

Branch:

```text
feature/book-metadata
```

Tasks:

- `Book.java`
- metadata-related methods in `LibraryService.java`
- metadata screen and save button in `MainFrame.java`

### Member 2

Branch:

```text
feature/book-reader-editor
```

Tasks:

- `FileManager.java`
- read/edit book content methods in `LibraryService.java`
- reader/editor screen in `MainFrame.java`

## Commit Examples

```bash
git add .
git commit -m "Add book model"

git add .
git commit -m "Add file manager"

git add .
git commit -m "Add book metadata UI"

git add .
git commit -m "Add book reader and editor"
```

## Pull Request Rule

Each member should create a pull request from their own branch to `main`.
The other member should review, approve, and merge the pull request.

## Team Workflow Update

The team used GitHub branches and pull requests to manage the final project updates.

Member 1 updated the README file and added final project notes.

Member 2 updated the team workflow guide and reviewed the repository structure.

Each pull request was reviewed and approved by the other member before being merged into the main branch.
